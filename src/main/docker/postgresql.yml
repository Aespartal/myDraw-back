version: '2'
services:
  mydraw-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/mydraw/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=mydraw
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432

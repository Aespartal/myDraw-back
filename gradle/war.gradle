apply plugin: "war"

bootWar {
    mainClassName = "com.project.mydraw.MydrawApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
